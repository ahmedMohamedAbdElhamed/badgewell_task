// Constants are used to directly contact with local storage keys
export class Constants {
  public static access_token = 'bwAccess_currentUser';
  public static refresh_token = 'bwRefresh_currentUser';
}
